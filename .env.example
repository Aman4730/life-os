# Copy to .env and paste your key. Also set this in Vercel → Settings → Environment Variables
# (Production + Preview). Never prefix this with VITE_ — it must stay server-side only.
GEMINI_API_KEY=your-gemini-api-key-here
